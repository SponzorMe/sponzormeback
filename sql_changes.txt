

/* Columna BOOLEAN para decir cuales son "featured" (aparecen en el home) */
ALTER TABLE usr ADD featured tinyint(1) NOT NULL DEFAULT '0' AFTER profile_picture; 

/* Featured iniciales (los del diseño) */
UPDATE usr SET featured=1 WHERE profile_picture LIKE 'http://sponzor.me/i%';

/* Cambiar username por fullname */
ALTER TABLE `usr` CHANGE `username` `fullname` VARCHAR( 255 ) CHARACTER SET utf8 COLLATE utf8_unicode_ci NOT NULL ;

/* Headline es la descripcion y debe permitir mas caracteres */
ALTER TABLE  `usr` CHANGE  `headline`  `headline` TEXT CHARACTER SET utf8 COLLATE utf8_unicode_ci NULL DEFAULT NULL ;